# pagina simulazioni ✅

Nuova versione implementata (sessione 2026-04-06):
- "Aggiungi voce" spostato in cima
- Griglia padre-figlio con sezioni collassabili (▶/▼)
- Colonne nutrienti dinamiche con footer di sezione e totale giornata
- Nome simulazione modificabile inline

# grafico ✅

Implementato (sessione 2026-04-06):
- `DailySimulationChart.vue`: bar chart, asse X = sezioni in ordine enum
- Toggle visibilità nutrienti (tutti visibili di default)
- Ordine sezioni: valore byte dell'enum (`Colazione=0, Pranzo=1, Cena=2, …`)

# Alimenti ✅

Clona aggiunto (sessione 2026-04-06):
- Tasto "Clona" in `FoodsList.vue`
- Feedback: scroll alla riga, highlight 2.5s, toast con link "Modifica" opzionale

# Nutrienti ✅

Implementato (sessione 2026-04-06):
- Tasto "Ordina" con modal drag & drop + fallback ↑/↓ (WCAG 2.1 AA)
- Nuovo nutriente = sempre ultimo in ordine (assegnato automaticamente lato backend)
- Ordinamento per `PositionOrder` già forzato ovunque tranne nella maschera (ordine alfabetico)
- Colonna "Qty default" sostituita con "Unità di misura default"

# Utenti ✅

Fix implementato (sessione 2026-04-06):
- `AdminUserService.GetUsersAsync`: sostituito `Task.WhenAll` con await sequenziale
- `UserManager.GetUsersInRoleAsync` usa lo stesso `DbContext` — operazioni concorrenti non supportate

# dipendenze npm

⚠️ Da fare in branch dedicato (`chore/update-dependencies`):
- `npx npm-check-updates -u && npm install && npm run build`
- `dotnet outdated` + `dotnet build`

# backend

⚠️ Pattern da `.github/instructions/` applicati inline durante la sessione 2026-04-06.
Eventuali gap residui sono tracciati in `docs/architecture-backend-findings.md`.
